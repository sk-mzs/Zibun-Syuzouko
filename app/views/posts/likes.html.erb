<div class="min-h-screen bg-gradient-to-b from-blue-50 to-white">
  <div class="container mx-auto px-4 py-16">
    <h1 class="text-4xl font-bold text-gray-800 mb-8 text-center">
      いいねした投稿
    </h1>

    <% if @like_posts.present? %>
      <div id="posts" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        <%= render @like_posts %>
      </div>
    <% else %>
      <div class="text-center py-12">
        <div class="bg-white rounded-lg shadow-lg p-8 max-w-md mx-auto">
          <p class="text-gray-600 mb-6">まだいいねした投稿がありません</p>
          <%= link_to "投稿一覧へ", posts_path, 
              class: "block w-64 mx-auto py-3 px-6 bg-primary text-white font-semibold rounded-lg shadow-md hover:bg-opacity-90 transition duration-300" %>
        </div>
      </div>
    <% end %>
  </div>
</div>